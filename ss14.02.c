#include <stdio.h>
#include <string>

int main() {
    char string[] = "Hello, World!";
    int length=strlen(string);
    for (size_t i = 0; i < length(); ++i) {
        std::cout << str[i] << " ";  
    }

    return 0;
}
